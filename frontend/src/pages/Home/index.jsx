import React from 'react';
import SnippetCard from '../../components/SnippetCard';

const Home = () => {
    return (
        <div className='home-page'>
            <SnippetCard/>
        </div>
    );
};

export default Home;